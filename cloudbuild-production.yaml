steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-f', 'Dockerfile.production',
      '-t', 'gcr.io/$PROJECT_ID/docpilot-streamlit:production',
      '.'
    ]
images:
  - 'gcr.io/$PROJECT_ID/docpilot-streamlit:production'
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'